// * connect MongoDB server in 
// ! CMD
// ? mongosh
// ! MongoDB Server Start PROT Is : 27017

// * show DataBase 
// ? show dbs

// * show collections in DataBase
// ? show collections

/*
# WHAT IS BSON ? : 
* => "Binary JSON" BSON's binary structure encodes tupe and length information, which allows it to be traversed much more quickly compared to JSON.
*/ 
